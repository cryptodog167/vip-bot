services:
  - type: web
    name: vip-bot
    env: python
    plan: free
    region: frankfurt
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    runtime: python
    pythonVersion: 3.10.11
    branch: main
    autoDeploy: true
